method For0 (n : nat)
  requires n > 0
{
  var q : nor := nor(n, 0);
  var p : nor := nor(n, 0);
  q[0 .. 1] *= H;
  for i in [0 .. n] with q[i .. (i + 1)]
    separates p[0 .. n]
  {
    p[0 .. n] *= λ (x => (x + 1) % 2);
  }
}
