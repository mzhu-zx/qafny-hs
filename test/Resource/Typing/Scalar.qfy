method TestScalarVariablesOk1(n : nat, m : nat)
  requires n >= 0
  requires m >= 0
{
  // Succeed
}

method TestScalarVariablesOk2(n : nat, m : nat, q : nat)  
  requires n >= 0
  requires m >= 0
  requires q <= 0
{
  // Succeed
}


method TestScalarVariablesFailNone(n : nat, m : nat)
  requires n >= 0
  requires m >= 0
  requires q <= 0
{
  // Fail
}

method TestScalarVariablesFailReturns(n : nat, m : nat) returns (q : nat)
  requires n >= 0
  requires m >= 0
  requires q <= 0
{
  // Fail
}

